const enum VaultProtocol {
    Teardown = "teardown",
    Jsonrpc = "jsonrpc",
    Ping = "ping",
    Error = "error",
    Reconnect = "reconnect",
    AckInit = "ackinit",
    ShowGenerator = "showgenerator"
}

export type Tokens = {
    // No need for these at the moment so avoiding that complexity until required
    // storage?: string,
    // forms?: string,
    // identity?: string,
    client?: string;
};
